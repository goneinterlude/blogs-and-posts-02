export type BlogInputDTO = {
    name: string,
    description: string,
    websiteUrl: string
}

export type BlogUpdateDTO = {
    id: number,
    name: string,
    description: string,
    websiteUrl: string
}