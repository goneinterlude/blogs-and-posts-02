export type BlogInputDTO = {
    name: string,
    description: string,
    websiteUrl: string
}

export type BlogUpdateDTO = {
    id: string,
    name: string,
    description: string,
    websiteUrl: string
}